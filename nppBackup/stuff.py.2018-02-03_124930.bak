#   python stuff.py
import pydub

def coroutine_play():
	start = yield
	sound = pydub.AudioSegment.from_file("No_Strings.mp4")
	pydub.playback.play(sound)
	
track = coroutine_play()
next(track)
track.send("start")