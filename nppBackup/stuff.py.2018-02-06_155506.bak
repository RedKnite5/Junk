#   python stuff.py



class person:
	def __init__(self, name):
		self.name = name
		
	def __repr__(self):
		return(self.name)
	
	
def polynomial_creator(*coefficients):
	def polynomial(x):
		res = 0
		for index, coeff in enumerate(coefficients):
			res += coeff*x**index
		return(res)
	return(polynomial)
	
binomial = polynomial_creator(1,1)

for i in range(5):
	print(binomial(i))















