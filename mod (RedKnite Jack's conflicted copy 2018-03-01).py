# list of used libraries

if __name__ == "__main__" and 1:
	import sys, math, random, pickle, sympy, hashlib, time, tkinter
	import itertools, tensorflow, cx_Freeze, os, datetime, timeit
	import urllib.request, numpy, _thread, string, colorsys
	import PIL, pydub, statistics, threading, re, webbrowser
	import email, selenium, cmath, bs4, platform, pylab, numba
	import tornado, twisted, turtle, pygame, pyglet, zipfile

	# import winsound 
	# import mahotas, appJar                          # doesn't work on fedora
	import enchant, curses, torch                 # doesn't work on windows
	# import graphics                               # doesn't work on either

	import scrapy, nltk, pandas, matplotlib, boto3, gensim, keras
	import pygame, pyglet, seaborn, keyboard, envelopes, arrow
	import plotly
	
	print("Success!")

r'''
cd C:\Users\Max\Documents\Python\Calculator
cd C:\Users\Max\Dropbox\Python

mod.py
'''

def lower_input(prompt=""):
	print(prompt)
	return(input().strip().lower())


def devar(x=None):
	return(x)
